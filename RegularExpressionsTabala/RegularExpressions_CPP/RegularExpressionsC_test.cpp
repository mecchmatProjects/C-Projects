// Задача номер 12, група "Комп'ютерна математика 2", Табала Матвій.
// Зніміть коментарі для того, щоб окремо запустити програму

/* #include "RegularExpressionsC.h" // Включаємо заголовний файл з реалізацією

using namespace RegularExpressionsC; // Використовуємо простір імен

int main() {
	const char* filename = "text.txt"; // Ім'я файлу, який будемо обробляти
	findHyperlinks(filename); // Знаходимо гіперпосилання
	convertDates(filename); // Конвертуємо дати
	convertNumbers(filename); // Конвертуємо числа
	replaceDatesAndTimes(filename); // Замінюємо дати та часи

	system("pause"); // Затримуємо консоль
	return 0;
} */
