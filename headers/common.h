#ifndef __COMMON__
#define __COMMON__

#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <limits.h>
#include <float.h>

#include <string.h>

#define SIGILL  1   /* Illegal instruction. */
#define SIGFNF 2 /* File not found. */
#define SIGIOR 3 /* Input/output error. */

typedef double DType; /* Double real type*/

typedef int IType; /* Int type*/

typedef unsigned NType; /* Unsigned type*/

typedef char ChType; /* Char type*/

#endif /* __COMMON__ end */
